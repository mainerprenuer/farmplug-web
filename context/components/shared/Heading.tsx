import Head from "next/head"

const Heading = () => {
  return (
    <Head>
        <title>Farmplug</title>
        <meta name="description" content="Farmplug - Get Fresh Fruits in a Click" />
        <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default Heading;